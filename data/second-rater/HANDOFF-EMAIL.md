# Handoff note to Akinruli Oluwaseun (draft — yours to send)

Subject: 20-minute favour — classify some design-system rules (blind)

Hi Akinruli,

I'm writing up a small study on how much of a design system's documented rules a
machine could actually check, and I need one independent person to classify a
sample so I can report an agreement score instead of just my own opinion.

It's about 20–40 minutes and needs no setup:

1. Open `blind-rating-sheet.csv` (attached). Each row is one real rule from a public
   design system (Primer, Carbon, Polaris, Spectrum, GOV.UK).
2. For each row, put one letter in the last column for the *kind of check* it would
   need — the eight options and a few tie-break rules are in `INSTRUCTIONS.md`
   (attached). Please read that first and don't look at anything else of mine, so
   your call is independent.
3. Do at least the first 40 rows; all 212 if you have the patience. Send the file back.

That's it — I'll compute the agreement from there. Genuinely helpful, thank you.

Hammed

---
Attach: `blind-rating-sheet.csv`, `INSTRUCTIONS.md`. Do NOT attach
`gold-classification.csv`, `adjudicated-classification.csv`, or `TIEBREAKS.md` —
those contain answers and would break the blind.
